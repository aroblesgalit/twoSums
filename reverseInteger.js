/**
 * @param {number} x
 * @return {number}
 */

var newNum = "";

var reverse = function(x) {
    for (var i = x.length - 1; i >= 0; i--) {
        return newNum.push[i];
    }
};

console.log(reverse(123));